# MECHANICUS Fine-Tuning Dataset

Supervised fine-tuning dataset for training a local Qwen model to encode editorial wisdom into MECHANICUS format.

## Files

| File | Purpose |
|---|---|
| `source-chunks.json` | 642 MECHANICUS chunks exported from Qdrant `text-and-seo` collection. Source of truth. |
| `gold-examples.jsonl` | 30 hand-crafted reference training examples (10 chunks × 3 input types). Used as few-shot in batch generation. Do not edit. |
| `batches/batch-NNN.jsonl` | Generated training batches, 15 chunks × 3 types = 45 examples each. Created per session. |
| `progress.json` | State: processed chunk IDs, batch manifest, next batch index. |
| `dataset.jsonl` | Final merged dataset built from gold + all batches. Regenerated by `scripts/merge-dataset.cjs` before training. |

## Format

ShareGPT JSONL, one training example per line:

```json
{
  "conversations": [
    {"from": "system", "value": "<SYSTEM_PROMPT>"},
    {"from": "human",  "value": "<input: source passage | question | brief idea>"},
    {"from": "gpt",    "value": "<single JSON object: {principle, explanation, domain, tags}>"}
  ],
  "meta": {
    "type": "T1|T2|T3",
    "domain": "ux|copy|seo|ui|mobile|perf|research",
    "source_chunk_id": "uuid-from-qdrant",
    "principle_head": "first 60 chars"
  }
}
```

Three human-input types per source chunk:

- **T1 — source passage**: 150-220 word simulated book excerpt that would produce this chunk.
- **T2 — user question**: practical query (can be Russian or English) that the chunk answers.
- **T3 — brief idea**: 1-2 line tight hint to be expanded into full MECHANICUS format.

All three types output the **same** chunk JSON — this trains the model to extract MECHANICUS from any input form.

## Session workflow (next batch)

Copy-paste this prompt into a new Claude Sonnet session:

```
Продолжаю сборку fine-tuning датасета для MECHANICUS encoder.

1. Прочитай data/finetune/README.md и data/finetune/progress.json
2. Прочитай data/finetune/gold-examples.jsonl (30 эталонов — твой стандарт качества)
3. Возьми следующие 15 чанков из data/finetune/source-chunks.json, которых нет в progress.processed_chunk_ids
4. Для каждого сгенерируй 3 training examples (T1, T2, T3) точно в формате эталонов
5. Запиши в data/finetune/batches/batch-{next_batch_index:03d}.jsonl
6. Обнови progress.json: добавь chunk_ids в processed, добавь запись в batches[], инкрементируй next_batch_index

Следуй стилю и длине из gold-examples.jsonl. T1 — 150-220 слов, T2 — один конкретный вопрос, T3 — 1-2 строки. Output chunk JSON — один объект без prose/fences/markdown.

verbose: false, отчитайся по завершении одним абзацем.
```

## Quality bar (for batch generation)

Each T1 passage must:
- Read like a real book (tone, flow, author voice implied)
- Be self-contained — practitioner could encode it without outside context
- Contain enough detail to justify every element of the output chunk
- Vary in surface form across the dataset (not all "From book X:" openers)

Each T2 question must:
- State a real practitioner problem, not a definition request
- Match the level of specificity of the answer (not "What is UX?" for a detailed chunk)
- Mix Russian and English roughly 50/50 across the dataset (target users write both)

Each T3 brief must:
- Compress the core idea in 1-2 lines
- Leave room for the model to add operators, antipatterns, and the `eg:` before >> after

Output chunk (the `gpt` value):
- Exact copy of the source chunk's `principle` / `explanation` / `domain` / `tags` fields
- Serialized as a single JSON object, no nesting inside a list
- No commentary, no markdown fences

## Target size

- 642 chunks × 3 examples = **1926 training examples** at completion
- Expected chunk loss from validation: ~5-10% (brittle chunks, format violations)
- Final usable dataset: **~1750-1830 examples**

This is comfortable for QLoRA fine-tune of Qwen2.5-7B or Qwen2.5-14B with rank 16, 3 epochs.

## Merging and training

Before training:

```bash
node scripts/merge-dataset.cjs
# produces data/finetune/dataset.jsonl = gold + all batches
```

Then pass `dataset.jsonl` to Unsloth / Axolotl / LLaMA-Factory. The `meta` field is stripped at training time — only `conversations` is consumed by the trainer.

## Validation checks

Run before committing any batch:

```bash
node scripts/validate-batch.cjs data/finetune/batches/batch-NNN.jsonl
```

Checks performed:
- Every line is valid JSON
- `conversations` has exactly 3 messages in order system → human → gpt
- `gpt.value` parses as JSON with required keys (principle, explanation, domain, tags)
- `principle` is 3-300 chars, `explanation` is 10-500 chars
- `domain` is in allowed set
- `meta.source_chunk_id` exists in `source-chunks.json`

Validation script is created on-demand in the session that first produces a batch.
